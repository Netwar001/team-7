{
  "name": "best-messenger",
  "version": "0.9.5",
  "scripts": {
	"postinstall": "pushd ./client/ && npm install"
    "start": "npm run build"
    "test": "#npm run test --if-present"
    "change": "mkdir ../server/client/"
    "move": "cp -r ./build/* ../server/client/"
    "clean": "popd"
    "change": "pushd ./server/"
    "install": "npm install"
    "clean": "popd"
  }
}